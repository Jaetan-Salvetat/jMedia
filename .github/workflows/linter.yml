on: [pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Setup
      - name: Clone repo
        uses: actions/checkout@master

      #Linter
      - name: Linter
        uses: natiginfo/action-detekt-all@1.23.5