on: [pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Setup
      - name: Clone repo
        uses: actions/checkout@master

      - name: set up JDK
        uses: actions/setup-java@v1
        with:
          java-version: 17

      #Linter
      - name: Linter
        run: ./gradlew detekt